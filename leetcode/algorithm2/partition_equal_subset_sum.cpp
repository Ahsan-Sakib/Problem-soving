#include<iostream>
#include<vector>
using namespace std;

bool canPartition(vector<int>& nums) {
    
}

int main(){
   vector<int> nums = {1,5,11,5};

   bool isPossible = canPartition(nums);
   cout<<isPossible<<endl;
}